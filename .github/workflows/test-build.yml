name: check-secret
on: [push]
jobs:
  check-secret:
    runs-on: ubuntu-latest
    steps:
      - name: check-secret
        shell: bash
        env:
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}
        run: echo "$TWINE_PASSWORD"
